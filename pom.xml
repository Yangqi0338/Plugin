<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.newzkl.platform</groupId>
    <artifactId>Plugin</artifactId>
    <version>${plugin.revision}</version>
    <packaging>pom</packaging>
    <name>Plugin</name>
    <description>Plugin - 身份 SPI 扩展点插件 (引用 Base biz, 按入口装配裁身份能力)</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <modules>
        <module>plugin-audit</module>
        <module>plugin-channel</module>
        <module>plugin-supplier</module>
        <module>plugin-operator</module>
        <module>plugin-hdh</module>
        <module>plugin-bi</module>
        <module>plugin-mmt</module>
        <module>plugin-openapi</module>
    </modules>

    <properties>
        <plugin.revision>1.0.0-SNAPSHOT</plugin.revision>
        <base.revision>1.0.1-SNAPSHOT</base.revision>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-boot.version>3.2.5</spring-boot.version>
        <graalvm.version>24.2.1</graalvm.version>
        <lombok.version>1.18.42</lombok.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <record-builder.version>44</record-builder.version>
        <mapstruct-plus.version>1.5.0</mapstruct-plus.version>
        <flatten-maven-plugin.version>1.6.0</flatten-maven-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>Base</artifactId>
                <version>${base.revision}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-channel</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-supplier</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-operator</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-audit</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-hdh</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-bi</artifactId>
                <version>${plugin.revision}</version>
            </dependency>
            <dependency>
                <groupId>com.newzkl.platform</groupId>
                <artifactId>plugin-mmt</artifactId>
                <version>${plugin.revision}</version>
            </dependency>

            <dependency>
                <groupId>org.graalvm.js</groupId>
                <artifactId>js-scriptengine</artifactId>
                <version>${graalvm.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.soabase.record-builder</groupId>
            <artifactId>record-builder-core</artifactId>
            <version>${record-builder.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>${flatten-maven-plugin.version}</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>io.soabase.record-builder</groupId>
                                <artifactId>record-builder-processor</artifactId>
                                <version>${record-builder.version}</version>
                            </path>
                            <path>
                                <groupId>io.github.linpeilie</groupId>
                                <artifactId>mapstruct-plus-processor</artifactId>
                                <version>${mapstruct-plus.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>${lombok-mapstruct-binding.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
